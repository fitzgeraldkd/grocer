import styled from 'styled-components';

export default styled.div`
  .button-container {
    text-align: right;
    grid-column: 1 / 3;

    button[type='submit'] {
      margin-right: 10px;
    }
  }
`;
